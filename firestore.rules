rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all collections and documents.
    // This is necessary because we have removed the authentication system.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
